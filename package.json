{
  "private": true,
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=26.8.1",
    "pnpm": ">=12.3.4"
  },
  "volta": {
    "node": "26.8.1",
    "pnpm": "12.3.4"
  },
  "scripts": {
    "generate:bindings": "pnpm --dir apps/desktop bindings",
    "typecheck": "pnpm generate:bindings && pnpm --dir apps/desktop/frontend typecheck",
    "lint": "pnpm --dir apps/desktop/frontend lint",
    "frontend:build": "pnpm --dir apps/desktop/frontend build",
    "frontend:test": "pnpm --dir apps/desktop/frontend test",
    "rust:fmt": "cargo fmt --check",
    "rust:check": "cargo check --workspace --all-targets",
    "rust:test": "cargo test --workspace",
    "storage:test": "cargo test --manifest-path firmware/esp32/Cargo.toml -p donder-device-storage --lib --locked",
    "rust:clippy": "cargo clippy --workspace --all-targets --all-features -- -D warnings",
    "check": "pnpm typecheck && pnpm lint && pnpm --dir apps/desktop/frontend knip && pnpm frontend:build && pnpm frontend:test && pnpm rust:fmt && pnpm rust:check && pnpm rust:test && pnpm storage:test && pnpm rust:clippy",
    "ci": "pnpm check",
    "bench:effect-vm": "pnpm bench:effect-vm:quick && cargo bench -p donder-language --bench effect_vm_bench && cargo bench -p donder-elaboration --bench render_bench",
    "bench:effect-vm:quick": "cargo bench -p donder-language --bench effect_vm_bench -- --quick && cargo bench -p donder-elaboration --bench render_bench -- --quick",
    "bench:effect-vm:save": "pnpm bench:effect-vm:quick && cargo bench -p donder-language --bench effect_vm_bench -- --save-baseline effect-vm && cargo bench -p donder-elaboration --bench render_bench -- --save-baseline effect-vm",
    "bench:effect-vm:compare": "pnpm bench:effect-vm:quick && cargo bench -p donder-language --bench effect_vm_bench -- --baseline effect-vm && cargo bench -p donder-elaboration --bench render_bench -- --baseline effect-vm",
    "build": "pnpm generate:bindings && pnpm --dir apps/desktop/frontend build",
    "tauri": "pnpm --dir apps/desktop exec tauri"
  }
}
